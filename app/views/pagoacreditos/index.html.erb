<% if params[:numero_hoja].nil? %>
<br />
<a class="nuevo" href ="#" data="/pagoacreditos/new" dataw="290"  datah="500px" title="Agregar nuevo Pago a Crédito">Agregar nuevo Pago Crédito</a>
<br /><br />
<div id="listado">
  <%  else %>
    <script type="text/javascript">
      limpiar_paginado();
    </script>
<% end %>
<table>
  <tr>
    <th class ="ui-state-active" width ="200">Nombre</th>
    <th class ="ui-state-active"></th>
    <th class ="ui-state-active"></th>
   
  </tr>

<% @pagoacreditos.each do |pagoacredito| %>
  <tr>
    <td  class="ui-state-default ui-corner-all"><%= pagoacredito.nombre %></td>
    <td  class="ui-state-default ui-corner-all"><a href ="#" data="<%= edit_pagoacredito_path(pagoacredito) %>" dataw="290"  datah="500px" class="list_link" title="Editar Pago a Crédito"><span class="ui-icon ui-icon-wrench"></span></a></td>
    <td  class="ui-state-default ui-corner-all"><a href="#" action="/pagoacreditos/<%=  pagoacredito.id %>" class="delete_list" data-method="delete" title="Eliminar Pago a Crédito"><span class="ui-icon ui-icon-trash"></span></a></td>
  </tr>
<% end %>
</table>
<%=  will_paginate @impuestos, :param_name => :numero_hoja , :class => :flickr_pagination %>
<% if params[:numero_hoja].nil? %>
</div>
<% end %>