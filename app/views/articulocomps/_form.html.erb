<%= form_for(@articulocomp) do |f| %>
  <% if @articulocomp.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@articulocomp.errors.count, "error") %> prohibited this articulocomp from being saved:</h2>

      <ul>
      <% @articulocomp.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <div class="field">
    <%= f.label :nombre %><br />
    <%= f.text_field :nombre %>
  </div>

  <div class="field">
    <%= f.label :descripcion %><br />
    <%= f.text_field :descripcion %>
  </div>
  
  <div class="field" >
    <%= f.label :productos %><br />
    <% for artic in Articulo.all %>
        <%= check_box_tag "articulocomp[articulo_ids][]", artic.id %>
        <%= label_tag "articulocomp[articulo_ids][]", artic.id, :for => "articulocomp[articulo_ids][]" %><br />
    <% end %>
  </div>
  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
