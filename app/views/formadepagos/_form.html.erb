<%= form_for(@formadepago) do |f| %>
  <% if @formadepago.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@formadepago.errors.count, "error") %> prohibited this formadepago from being saved:</h2>

      <ul>
      <% @formadepago.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br />
    <%= f.text_field :nombre %>
  </div>  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
