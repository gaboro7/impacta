<% if params[:numero_hoja].nil? %>
<br />
<a class="nuevo" href ="#" data="/pagocontados/new" dataw="290"  datah="500px" title="Agregar nuevo pago contado">Agregar nuevo pago contado</a>
<br /><br />

<div id="listado">
  <%  else %>
    <script type="text/javascript">
      limpiar_paginado();
    </script>
<% end %>

<table>
  <tr>
    <th class ="ui-state-active" width ="200">Nombre</th>
    <th class ="ui-state-active"></th>
    <th class ="ui-state-active"></th>
   
  </tr>

<% @pagocontados.each do |pagocontado| %>
  <tr>
    <td  class="ui-state-default ui-corner-all"><%= pagocontado.nombre %></td>
    <td  class="ui-state-default ui-corner-all"><a href ="#" data="<%= edit_pagocontado_path(pagocontado) %>" dataw="290"  datah="500px" class="list_link" title="Editar pago contado"><span class="ui-icon ui-icon-wrench"></span></a></td>
    <td  class="ui-state-default ui-corner-all"><a href="#" action="/pagocontados/<%=  pagocontado.id %>" class="delete_list" data-method="delete" title="Eliminar pago contado"><span class="ui-icon ui-icon-trash"></span></a></td>
  </tr>
<% end %>
</table>
<%=  will_paginate @impuestos, :param_name => :numero_hoja , :class => :flickr_pagination %>
<% if params[:numero_hoja].nil? %>
</div>
<% end %>