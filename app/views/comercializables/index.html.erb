<h1>Listing comercializables</h1>

<table>
  <tr>
    <th>Descuento</th>
    <th>Garantia</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @comercializables.each do |comercializable| %>
  <tr>
    <td><%= comercializable.descuento %></td>
    <td><%= comercializable.garantia %></td>
    <td><%= link_to 'Show', comercializable %></td>
    <td><%= link_to 'Edit', edit_comercializable_path(comercializable) %></td>
    <td><%= link_to 'Destroy', comercializable, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Comercializable', new_comercializable_path %>
